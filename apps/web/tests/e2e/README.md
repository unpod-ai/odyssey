# tests/e2e — deliberately empty

No Playwright/browser-based e2e runner is wired up yet — see the parent
`README.md`'s "Tests" section for how this app was actually verified
(a real `services/api` instance + `curl` against `npm run dev`, not a
browser). Add real Playwright specs here if that verification path stops
being sufficient, not before.
