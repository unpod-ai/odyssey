"""odyssey-dataprep — data_preparation stages.

Only ``normalization`` exists so far. See ``docs/STRUCTURE.md`` for the full
planned pipeline (collection -> cleaning -> normalization -> annotation ->
augmentation -> validation -> splitting).
"""

from __future__ import annotations

__all__: list = []
