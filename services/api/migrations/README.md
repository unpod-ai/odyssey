# migrations/ — deliberately empty

`docs/STRUCTURE.md` describes alembic migrations here, for the
`postgres.py` repository named in the same tree. Neither exists yet
(`repositories/__init__.py` explains why) — there is no relational schema
to migrate. Add real alembic revisions here the same commit a Postgres-
backed repository is actually built, not before.
